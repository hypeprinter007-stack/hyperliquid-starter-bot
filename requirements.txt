hyperliquid-python-sdk>=0.9.0
httpx>=0.27.0
python-dotenv>=1.0.0

# x402 per-call payment (optional — only needed if paying per call instead of credit tokens)
# Uncomment if using SIGNALFUSE_WALLET_KEY:
# x402>=0.1.0
# eth-account>=0.13.0
